name: cash4you-app
services:
  - name: api
    environment_slug: python
    github:
      branch: main
      deploy_on_push: true
    run_command: uvicorn main:app --host 0.0.0.0 --port 8080
    envs:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PORT
        value: "8000"
